# Задание новому агенту — Али-Баба и 40 кладов

Ты продолжаешь **форк** игры HCG. Репозиторий этой копии — архив `ALI-VPS.zip`.
Оригинал HCG (`/var/www/hcg`, порт 3001, бот @hcg1_bot) **не менять**.

## Кто ты

Полезный агент на Arena.ai. Отвечай по-русски, комментарии в коде — английский.
Пользователь не программист: точные команды для PowerShell и для PuTTY.

## Референс стиля (приложен к чату)

Файл `docs/refs/character-style.png` и картинки из первого сообщения пользователя.

- Disney 1992 *Aladdin*: дворец Аграбы ночью, золотые арки, полумесяц, бирюзовая магия, ковры, герой в бирюзово-золотом жилете с золотым ключом.
- Киберпанк, неон `#00FFAA`, шрифт Orbitron — **запрещены**.
- Палитра: золото `#D4AF37`, ночь `#0B1426`, песок `#F5E6C8`, бирюза магии, рубин ловушек.
- Шрифты: Cinzel / Cormorant Garamond.
- Графику для кириллицы рисовать кодом (PIL), не нейросетью.

## Что уже сделано в этом архиве

- Копия сервера, бота, парсера прокси.
- Контейнеры: `ali_app`, `ali_bot`, `ali_proxy`. Том: `ali-data`.
- Порт хоста: **127.0.0.1:3002 → 3000**.
- Каталог на VPS: `/var/www/ali`.
- Экраны из репозитория [ALI2.0](https://github.com/bez1655/ALI2.0.git) уже лежат в `public/`:
  - поле: `BoardALI.mp4` + постер `BoardALI.png`
  - вход: `LogALI.mp4` + постер `LogALI.png`
  - правила: `RulesALI.mp4` + постер `RulesALI.png`
- Дополнительно: `help-bg.jpg`, `Dice.png`, `admin-bg.png`, `chips/`.
- Код смотрит именно на `*ALI.*` (`?v=2`). Старые `BoardHAP` / `Log.mp4` / `RulesHAP` больше не используются.
- 65 клеток, те же `id`, `type`, `value`, `x`, `y`. Переименованы под сказку (`src/game/data/cells.json`).
- Экран входа переписан (форма поверх PNG, не старые пиксельные поля от кибер-видео).
- Доска и правила читают PNG, не video.
- Скрипт `deploy/ВСЁ-НА-VPS.sh` переименован под `ali_*` и `/var/www/ali`.
- `.env.example` — отдельные секреты и бот.

## Что доделать в новом чате (по приоритету)

1. Прогнать тексты бота (`bot/src/index.ts`): команды, справка, «ход», «приз» → язык сказки (визирь, гонец, клад, кубик судьбы).
2. `HelpScreen.tsx`, `AdminConsole.tsx`, `TurnOutcomeCard`, `cellGuide.ts`, `rollReport.ts` — убрать «кредит», «крипто», «энергокристалл», «кибер».
3. Цвета UI `#00ffaa` массово заменить на `#D4AF37` (доска ещё частично мятная).
4. Проверить `assets.test.ts` (нет mp4; `?v=` одинаковый).
5. APK: `apk/capacitor.config.json` — имя приложения, иконка с референса.
6. Подписи на клетках: координаты те же 65 клеток. Новое поле `BoardALI` — сверять посадку фишек калибровкой в админке, не двигать json без сверки.
7. Не деплоить поверх HCG. Не форс-пушить GitHub `main` HCG.

## Инфра

- Домен (план): `https://ali.bez12.store` → Caddy → `127.0.0.1:3002` (`deploy/CADDY.md`).
- Новый бот, новая группа, новые `SESSION_SECRET` / `INTERNAL_API_SECRET` / `ADMIN_PASSWORD_HASH`.
- Firestore — другая база, иначе состояние игроков смешается с HCG.

## Правила работы

- Баги чинить с проверкой: воспроизвести → упасть → починить → пройти.
- `npm run test:e2e` только на Node ≥22; в песочнице часто Node 20.
- Коммиты: `git -c user.name="ALI" -c user.email="ali@local"`.
- Эксплойты не писать. Секреты в чат не просить целиком.

## Команды админа (кратко)

См. `АДМИН-ПЛАН.md`. Старт:

```bash
export ALI_DIR=/var/www/ali
bash deploy/ВСЁ-НА-VPS.sh
```

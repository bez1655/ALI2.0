# Али-Баба и 40 кладов

Отдельная игра. HCG (`/var/www/hcg`, порт 3001) не трогать.

```bash
cd /var/www
mkdir -p ali-new
unzip -o ~/ALI-VPS.zip -d ali-new
export ALI_DIR=/var/www/ali
bash ali-new/deploy/ВСЁ-НА-VPS.sh
```

Экраны: `public/BoardALI.*`, `LogALI.*`, `RulesALI.*` (png + mp4 из репозитория ALI2.0).

Дальше — `АДМИН-ПЛАН.md` (по шагам) и `ПЕРЕНОС-В-НОВЫЙ-ЧАТ.md` (для агента).

# План для администратора — Али-Баба и 40 кладов

Делайте по порядку. HCG не останавливайте.

## 0. Что купить / завести заранее

1. **Поддомен** `ali.bez12.store` (или другой) → A-запись на IP VPS.
2. **Новый Telegram-бот** у [@BotFather](https://t.me/BotFather):
   - `/newbot`
   - имя: `Али-Баба и 40 кладов`
   - username: например `ali40kladov_bot`
   - скопируйте токен `123456:ABC...`
   - **нельзя** вставлять токен `@hcg1_bot`
3. **Новая закрытая группа** для журнала ходов (или отдельный топик).
   - добавьте бота в группу, дайте право писать
   - узнайте id: перешлите любое сообщение группы боту [@userinfobot](https://t.me/userinfobot)  
     id группы обычно `-100…`
4. **Firestore** — либо новый проект Firebase, либо **вторая база** в том же проекте.
   - [Firebase Console](https://console.firebase.google.com/) → Project settings → Service accounts → Generate new private key
   - ключ — одна строка JSON в `FIREBASE_SERVICE_ACCOUNT`
   - `FIREBASE_FIRESTORE_DATABASE_ID` задайте своё имя, не то же, что у HCG

## 1. Залить архив

На компьютере (PowerShell), путь к zip подставьте свой:

```powershell
scp C:\Users\ВАШЕ_ИМЯ\Downloads\ALI-VPS.zip root@ВАШ-СЕРВЕР:/root/
```

Сервер: `root@VM-480084` (как у HCG) или тот, что у вас сейчас.

## 2. Распаковать РЯДОМ с HCG

В PuTTY:

```bash
cd /var/www
mkdir -p ali-new
unzip -o /root/ALI-VPS.zip -d ali-new
ls ali-new/deploy/ВСЁ-НА-VPS.sh
```

Не распаковывать внутрь `/var/www/hcg`.

## 3. Создать .env (самое важное)

```bash
mkdir -p /var/www/ali
cd /var/www/ali
cp /var/www/ali-new/.env.example .env
```

Сгенерировать три секрета (на сервере, один раз):

```bash
cd /var/www/ali-new
# если нет node — возьмите контейнер:
docker run --rm -v /var/www/ali-new:/app -w /app node:22-bookworm-slim \
  bash -lc "npm ci && npm run hash-password -- 'ВАШ-СИЛЬНЫЙ-ПАРОЛЬ-АДМИНА'"
```

Команда напечатает `ADMIN_PASSWORD_HASH`, `SESSION_SECRET`, `INTERNAL_API_SECRET`.

Впишите в `/var/www/ali/.env` через `nano /var/www/ali/.env`:

| Ключ | Что поставить |
|---|---|
| `WEB_APP_URL` | `https://ali.bez12.store` |
| `ADMIN_LOGIN` | `admin` |
| `ADMIN_PASSWORD_HASH` | из команды выше |
| `SESSION_SECRET` | из команды выше, **не** как у HCG |
| `INTERNAL_API_SECRET` | из команды выше, **не** как у HCG |
| `TELEGRAM_BOT_TOKEN` | токен **нового** бота |
| `TELEGRAM_ADMIN_USERNAME` | `@bez_adm11` или ваш |
| `TELEGRAM_GROUP_CHAT_ID` | `-100…` новой группы |
| `TELEGRAM_PROXY` | пусто `""` если Telegram открыт |
| `BOT_USE_PROXY` | `0` |
| `FIREBASE_SERVICE_ACCOUNT` | JSON **одной строкой** |
| `FIREBASE_FIRESTORE_DATABASE_ID` | своё имя базы |

Права:

```bash
chmod 600 /var/www/ali/.env
```

Проверка: в `.env` **нет** токена `@hcg1_bot` и нет тех же трёх секретов, что в `/var/www/hcg/.env`.

## 4. Caddy

См. `deploy/CADDY.md`. Добавить сайт на порт **3002**. Reload Caddy.

## 5. Запуск контейнеров

```bash
export ALI_DIR=/var/www/ali
bash /var/www/ali-new/deploy/ВСЁ-НА-VPS.sh сервер
```

Должны появиться `ali_app`, `ali_bot`, `ali_proxy`.  
`hcg_app` / `hcg_bot` продолжают работать.

Проверка:

```bash
curl -s http://127.0.0.1:3002/healthz
docker ps --format '{{.Names}}'
```

## 6. Бот в Telegram

1. Напишите новому боту `/start`
2. Mini App URL = `https://ali.bez12.store`
3. Проверьте `/help` под админом

## 7. Экраны (уже в архиве)

В `public/` уже лежат файлы из [ALI2.0](https://github.com/bez1655/ALI2.0.git):

| Экран | Видео | Картинка (постер) |
|---|---|---|
| Поле | `BoardALI.mp4` | `BoardALI.png` |
| Вход | `LogALI.mp4` | `LogALI.png` |
| Правила | `RulesALI.mp4` | `RulesALI.png` |

Ничего копировать руками не нужно. Если захотите заменить — кладите файлы **с теми же именами** в `/var/www/ali/public/` и пересоберите:

```bash
cd /var/www/ali
docker compose up -d --build ali_app
```

## 8. APK (по желанию)

Пункт 3 в меню `ВСЁ-НА-VPS.sh`. Нужны Node 22 и JDK 21.  
Готовый файл: `/var/www/ali-apk/`

## Если что-то сломалось

```bash
bash /var/www/ali/deploy/ВСЁ-НА-VPS.sh бот
bash /var/www/ali/deploy/ВСЁ-НА-VPS.sh статус
docker logs --tail 50 ali_bot
```

HCG откатывать не нужно — это другой том `ali-data`.

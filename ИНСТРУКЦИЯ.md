# HCG — инструкция

## Залить архив (на своём компьютере, PowerShell)

```powershell
scp C:\путь\HCG-VPS.zip root@ВАШ-СЕРВЕР:/root/
```

## На сервере (PuTTY)

```bash
cd /var/www
mkdir -p hcg-new
unzip -o /root/HCG-VPS.zip -d hcg-new
bash hcg-new/deploy/ВСЁ-НА-VPS.sh
```

Архив нельзя распаковывать внутрь `/var/www/hcg`. Только рядом.

Данные игроков лежат в томе Docker `hcg_hapstore-data`, не в папке проекта.

## После обновления кода

```bash
cd /var/www/hcg
bash deploy/ВСЁ-НА-VPS.sh пересобрать
```

Парсер `hcg_proxy` не останавливается.

## Если бот молчит

```bash
bash deploy/ВСЁ-НА-VPS.sh бот
```

## Откат клетки игрока

Сначала сверка, потом правка руками в админке:

```bash
bash deploy/ВСЁ-НА-VPS.sh игрок hapalka228
```

## Бот напрямую

```bash
bash deploy/ВСЁ-НА-VPS.sh напрямую
```

Ставит `BOT_USE_PROXY=0`. Сборщик прокси остаётся для команды `/proxies`.

## Типовые ошибки

| Сообщение | Что делать |
|---|---|
| docker не установлен | Вы не на том сервере |
| нет .env | `cd /var/www/hcg && npm run setup-env -- --password 'пароль'` |
| Telegram 401 | Новый токен у @BotFather, вписать в `.env`, пункт 12 |
| Кнопок нет | Новая команда `/start` в Telegram |
| permission denied | `bash scripts/fix-permissions.sh` |

Секреты живут только в `.env` на сервере. В чат их не слать.
